# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# e2e test runtime artefact (written by tests/_streamlit_lifecycle.py)
tests/_streamlit_restart_marker.txt

# Virtual env
.venv/
venv/

# Streamlit
.streamlit/secrets.toml

# Project working data
data/logs/*
data/output/*
data/input/*
!data/logs/.gitkeep
!data/output/.gitkeep
!data/input/.gitkeep

# IDE
.vscode/
.idea/
*.swp

# Claude Code local settings
.claude/

# Codex / other-agent local artifacts — auto-generated per-repo mirror noise.
# Cross-agent instructions are maintained in CLAUDE.md (AGENTS.md is a committed
# one-line pointer to it, deliberately NOT ignored). Real Codex config: ~/.codex/.
.agents/
.codex/

# Launcher scratch (ephemeral images/logs written by the tray launcher)
.launcher-tmp/

# Runtime log files, including the tray's webapp/watchdog.log breadcrumb trail
# (app/tray/watchdog.py's BreadcrumbLog + its rotated .log.1). See
# docs/windows-tray.md gotcha #5 — these are per-machine diagnostics, never
# committed.
*.log
*.log.1

# OS
.DS_Store
Thumbs.db
